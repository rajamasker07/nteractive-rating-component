/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  distDir: 'dist',
  basePath: '/nteractive-rating-component',
  images: {
    unoptimized: true
  }
};

export default nextConfig;
